{
  "devDependencies": {
    "autoprefixer": "^9.7.6",
    "gh-pages": "^2.2.0",
    "postcss-cli": "^7.1.0",
    "postcss-import": "^12.0.1",
    "postcss-nested": "^4.2.1",
    "purgecss": "^2.1.2",
    "tailwindcss": "^1.2.0"
  },
  "scripts": {
    "build": "JEKYLL_ENV=production bundle exec jekyll build",
    "deploy": "JEKYLL_ENV=production bundle exec jekyll build && gh-pages --branch master --dist _site"
  }
}
